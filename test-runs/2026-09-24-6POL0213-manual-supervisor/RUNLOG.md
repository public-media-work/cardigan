# Run log — 6POL0213 manual supervisor run (2026-09-24)

Code under test: worktree at origin/main e142891 (identical to what prod runs; neither #407 nor #409 applied).
Subagent models: haiku (analyst/seo/validator), sonnet (formatter/timestamp). Prod defaults: haiku-4.5 / sonnet-4.6.

## Intake
- SST record found (rectgQzS2b3chVnro). Host/Presenter/General Keywords fields empty on record -> prod regex extraction fills host=Shawn Johnson, presenter=Zac Schultz, Anya van Wagtendonk, Rich Kremer (from Project Notes).
- **NEW DEFECT (SST-1):** `SST_CONTEXT_FIELD_MAP["social_media_description"] = "Social Media Description"` — Airtable returns 422 "Could not find a field with name or ID" for that name. `fields.get()` returns None silently so the value never reaches any prompt. The comment on the map claims tests pin it against the schema; the pin is stale or the snapshot is wrong.
- Duration 18.07 min (SRT), 373 captions, 385 after `>>` interior split (12 captions split).
- `job.word_count` (main `calculate_transcript_metrics`) = **4825**; dialogue words = **3333**; gap 1492 = 373 × 4 exactly. Confirms peer datum (c) and #404's inflation finding on a second job. Branch #404 `is_srt=True` gives 3333.
- content_type=full; timestamp phase auto-triggers (>=10 min).
- **Chunking on main: 2 chunks** (3333 >= threshold 3000; target 2000). Same path as job 47 (6POL0212) which paused on a "(section 1 of 2)" header leak. So the seam-risk case IS exercised.
- **Chunking on #404 branch: single call** (budget 6553 words at 16384 cap, tokens_per_word 2.0). Even at tokens_per_word 1.2–2.0 stays single. So #409 changes this job's path from 2 chunks to 1 call.
- Roster resolution (what `_finalize_with_qa_gate` escalation would pick today): sonnet -> anthropic/claude-sonnet-5, opus -> anthropic/claude-opus-5.5, haiku -> anthropic/claude-haiku-4.5.
- `_load_agent_prompt` called without model: `{model name you are running as}` left unfilled in the analyst system prompt (existing bug, confirmed).

## Phases

### OpenRouter replay — single-call formatter prompt, anthropic/claude-sonnet-5, max_tokens=16384
- Run 1: HTTP 200, `message.content = null` (script crashed before saving raw; the only evidence is the TypeError). Consistent with the entire budget going to reasoning.
- Run 2: HTTP 200, provider Amazon Bedrock, **finish_reason="length"**, native "max_tokens". prompt_tokens 32,816; completion_tokens 16,384 of which **reasoning_tokens 12,575** (77%). Content: 11,970 chars / 2,044 content words. Cost $0.2295, 148 s.
  - tokens per content word INCLUDING reasoning: 8.02. EXCLUDING reasoning: (16384-12575)/2044 = **1.86** → peer's 1.77/2.0 calibration is right for content tokens; reasoning is the confound.
  - Output stops mid-sentence at "How is Tom" (≈10:54). Also contains a `<!-- REVIEW NOTES` block (model flagged the unattributable group "Hello").
  - **Prod gates on this output:** completeness 61.3% → `[truncation]` pause; seam gap 00:10:54–00:18:02 (144 captions) BLOCKING → would also pause. Review-notes marker present → would route non-fixable at QA.
- Run 3 (`"reasoning": {"enabled": false}` added to payload, everything else identical): **finish_reason="stop"** (end_turn), completion_tokens 6,023, reasoning_tokens 0, 3,344 content words → **1.80 tokens/word**, full `# Formatted Transcript` header + Duration 00:18:04 + `**Status:** ready_for_editing`, no review notes; completeness 100.3% PASS, no seam gap; 56.5 s; $0.126. Single-call formatter on sonnet-5 is fully viable once reasoning is controlled.

  | Replay | reasoning | finish | completion tok | reasoning tok | content words | tok/word (content) | gates |
  |---|---|---|---|---|---|---|---|
  | 1 | default | (content null) | — | — | 0 | — | crash |
  | 2 | default | length | 16,384 | 12,575 | 2,044 | 1.86 | truncation + seam pause |
  | 3 | disabled | stop | 6,023 | 0 | 3,344 | 1.80 | pass |

- **Defect (LLM-1, next-version blocker):** `_call_openrouter` sends no `reasoning` control. Modern Anthropic models (sonnet-5, opus-5.5 — exactly what `model_roster.newest_in_family` resolves to for escalation TODAY) reason by default on OpenRouter, and reasoning tokens are drawn from `max_tokens`. So: (1) #407's `finish_reason=="length"` check WILL fire (validated), but (2) #409's word budget from `max_tokens/tokens_per_word` is wrong for these models — effective content budget was 3,809 tokens, not 16,384 — and (3) prod's auto-escalation to sonnet-5/opus-5.5 on main (no max_tokens at all → provider default) is a plausible cause of the `[qa_fail] QA failed after escalation` pauses on jobs 43/44/47 (timestamp phase escalates sonnet→opus). Fix: send `"reasoning": {"enabled": false}` (or an explicit reasoning budget) on OpenRouter payloads, and/or subtract reasoning budget from the output budget; log `completion_tokens_details.reasoning_tokens`.

### analyst (haiku; prod default haiku-4.5 on openrouter-cheapskate)
- Output 16.5 KB / 2326 words, all required sections present, no fences, no preamble. Model line self-filled as `claude-haiku-4-5-20251001` because the placeholder is never substituted by the worker.
- **Defect (AN-1):** Structural Breakdown act timings are invented: acts run "17:00-24:00", "24:00-28:00", "28:00-30:00", "30:00-30:30" on an 18:04 episode, while the same document's Review Items say "SRT file spans 0:00 to 18:04". The analyst sees raw SRT timecodes and still produced a 30-minute structure. Timestamp phase only receives `analysis[:4000]` so it may not see these, but SEO/validator see the whole thing. No gate checks analyst timings against duration (style post-stage is off).
- Review Items all ticked; no review notes carried forward. No intervention needed.

### seo (haiku; prod default haiku-4.5 on openrouter-cheapskate)
- 15.6 KB, `# SEO Report` structure complete, `**Status:** ready_for_review`.
- **Defect (SEO-1 = #309 reproduced):** the Short Description section contains the model's drafting trail in the deliverable: "This revision (113 chars - exceeds limit, revised below): … Still 130 chars. Final revision to meet 90-char limit: …" and Next Steps repeats an over-limit variant with "(108 chars — exceeds limit, use 90-char version)". Self-reported char counts are unreliable (see measured table below). A validator reading this will (correctly) flag contradictory/over-limit metadata even though a compliant value exists. Nothing deterministic strips the trail (style post-stage off; no "paste-ready value" extractor for SEO other than #384's keyword fix).
- **Defect (SEO-2, root cause of #309):** measured lengths of the *recommended* values vs the model's own claims:

  | Field | Limit | Model claims | Measured |
  |---|---|---|---|
  | Title | 80 | "80 characters exactly" | **88** |
  | Short description | 90 | "88 characters" | **115** |
  | Long description | 350 | "340 characters" | **596** |

  All three over limit; all three self-counts wrong by 10–70%. On being asked afterwards, the SEO subagent re-counted and reported "short description 114 (I incorrectly stated 88)… title meets 80 exactly… long description meets 350" — still wrong on two of three. Haiku cannot count characters; the prompt asks it to, and nothing deterministic enforces the limits (post-stage `seo.*_over_limit` truncation is `off`, lint is `off`). The validator (also Haiku) is then asked to judge these counts. This is a mechanical check that should never be delegated to an LLM.
- SEO prompt only receives `formatter_output[:2000]` — with FMT-1 that 2,000 chars is dialogue with no header, so SEO never sees Project/Program from the transcript (gets them from SST instead; fine here, brittle without SST).

### validator (haiku; prod default haiku-4.5 on openrouter)
- Output wrapped in ```json fences; `_parse_validation_result` strips them → parsed OK.
- Verdict: analyst pass; **formatter FAIL** (missing header block, missing REVIEW NOTES block, missing Status footer — i.e. FMT-1, caught correctly); **seo FAIL** (title 88 > 80, short description 113 > 90 — SEO-2, caught correctly; validator's own counts 88/113 vs measured 88/115). overall = fail. Did NOT flag the long description (596 > 350) — missed one.
- **QA-gate routing (prod code, `classify_qa_failure`):** the formatter flag text contains "missing <!-- REVIEW NOTES: ... --> block" → substring "review note" matches `NONFIXABLE_FLAG_PATTERNS` → classified NON-FIXABLE → `escalate=False` → **prod pauses `[qa_review]`**: "Paused for human review — the formatter flagged items it can't verify and a stronger model won't resolve: Formatter contract violation: missing required metadata header block… Verify media_id + proper-noun spelling, then resume."
- **Defect (QA-1, likely the single biggest contributor to the paused backlog):** non-fixable classification is a case-insensitive substring match over free-text flag prose. Any validator sentence that *mentions* review notes, "unidentified", "not identified", "unverified", "excerpt", "media id" etc. is treated as a human-review item, even when the flag is "the model forgot X" — which a re-run fixes. Here a purely mechanical, model-fixable omission is routed to a human with a message about media_id and proper nouns that has nothing to do with the actual problem. And because ANY non-fixable flag suppresses escalation, the genuinely fixable SEO flags never get their escalation pass either. Recall prod's paused list: jobs 29–47 are "nearly all `[qa_review]`", most citing review-notes text — consistent with this misroute.
- **Defect (VAL-1):** validator treats the REVIEW NOTES block as *required* ("missing <!-- REVIEW NOTES: ... --> block immediately after header"). formatter.md:91 says `<!-- REVIEW NOTES (only if needed):` and :117 "only if there are real issues". So this part of the flag is a validator false positive — and it is the exact phrase that trips QA-1. A transcript with no problems must NOT carry review notes, yet the validator penalises its absence and the gate then routes the job to a human.
- **Defect (RETRY-1):** `_run_formatter_chunked` builds its own user messages and never reads `context["_validation_flags"]`, `_previous_output` or `_editorial_feedback` (grep of worker.py:2692–2905: zero hits; only `_build_phase_prompt` at :3185 does). So for any job that chunks (≥3,000 dialogue words on main — every dense 18-min IWP episode), a phase retry with editor feedback, and the QA-gate escalation's "previous flags" context, are silently dropped. The retry is a blind re-run on a different model with the identical prompt that produced the defect.

### timestamp (sonnet; prod default sonnet-4.6 on openrouter) — runs after the 4 phases, before the QA gate
- 1,163 B. 7 chapters (cap for <30 min is 7 — at the cap, fine), Media Manager table + YouTube list, first chapter "0:00 Episode intro", last chapter ends 0:18:04.000 (exact SRT end). Titles are down-style, descriptive. **PASS, no intervention.** No timestamp defect found on this episode with a modern Sonnet.
- Pre-stage text says "first chapter … (added automatically)" and "final chapter ends at exactly 18:04 (handled automatically)" while the post-stage that would do it is `off`; the model did both itself anyway. Misleading wording, harmless here.
- **Observation (OBS-1):** the validator never receives timestamp output (validator prompt = analyst + formatter + seo only). So prod's paused jobs showing phase=`timestamp` with `[qa_fail]`/`[qa_review]` (jobs 39, 42, 43, 44, 47) are NOT timestamp failures — `current_phase` just holds the last phase that ran before `_finalize_with_qa_gate` paused the job. The dashboard's "paused at timestamp" attribution is misleading; the real failing phase is in `validation_result`.

### Escalation pass (replicating `_finalize_with_qa_gate` as if QA-1 had not misrouted)
- `select_escalation_phases` → earliest flagged = formatter → rerun formatter + seo (validator excluded), then re-validate on default validator model.
- Formatter: sonnet → **opus** (roster would pick anthropic/claude-opus-5.5). Chunked path again (2 chunks), identical prompts (RETRY-1: no flags passed). SEO: haiku → **sonnet** (roster: anthropic/claude-sonnet-5), identical prompt.
- Prediction before running: FMT-1 recurs (prompt conflict is model-independent) → re-validate fails → prod result `[qa_fail] QA failed after escalation` (jobs 43/44/47 pattern).
- **Result (opus chunks, merged 21,292 chars):** prediction half wrong. Opus chunk0 DID emit the header (Project/Program/Duration 00:18:04/Date) and chunk1 DID emit `**Status:**` — so FMT-1 is model-sensitive (Sonnet reads "format only the dialogue" as no-header; Opus does not). But Opus resolved the same prompt ambiguities the other way: it added a 6-item `<!-- REVIEW NOTES -->` block (caption fixes "Arnie"→"Anya", "the U. Lines"→"the Uihleins", group "Hello" labelled "Panelists", and two seam-adjacent turns it "could not confidently attribute" → **`**Speaker 1:**` / `**Speaker 2:**`**), and set **`**Status:** needs_review`**. Deterministic gates: completeness PASS (3314/3333), seam clean. Contract markers present → `classify_qa_failure` marks EVERY formatter flag non-fixable → re-validation fail is guaranteed to end as `[qa_fail] QA failed after escalation`. This is job 45's pause text ("three speakers labeled as unidentified: 'Host', 'Speaker 1', 'Speaker 2'") reproduced on a different episode with a stronger model.
- Attribution disagreement between runs (no gate can see this): donor/Wikler answer — analyst says Zac, Sonnet gave it to Zac, Opus gave it to Anya. Closing Hong unity-tour answer — Sonnet: Zac, Opus: Anya. Speaker identity in `>>`-only captions is being inferred from the analyst's guess, then "verified" against that same guess.
- **Escalated SEO (sonnet):** 10,243 B. Measured: title 79 (≤80 ✓), short description 74 (≤90 ✓), long description 339 (≤350 ✓). No drafting trail. Sonnet can count; Haiku cannot. But `**Status:** draft` — job 44 was failed by the validator for "SEO metadata status marked 'draft'".
- **Defect (SEO-3):** the SEO user prompt contains only `formatter_output[:2000]` (cuts off mid-sentence in Rich Kremer's first answer) and the style pre-stage's "Keyword candidates verified present in the transcript" line is derived from that same truncated excerpt: it lists "Shawn Johnson, Johnson, Rich Kremer, Kremer, Schultz" and omits Anya van Wagtendonk, Crowley, Tiffany, First Amendment LLC. The Sonnet SEO agent therefore downgraded Crowley/Tiffany/van Wagtendonk/"mega-donors" to "Source: implied" and hedged on the episode's main keywords. The phase that most needs the full transcript for keyword grounding gets 2 KB of it, and the "verified" list actively tells it the rest is unverified.
- **Defect (VAL-2):** prompts/seo.md:247 defines the SEO footer as `**Status:** {draft | ready_for_review | approved}`. `draft` is a legal value. Prod job 44's validator flag "SEO metadata status marked 'draft'…" is therefore a false positive against the SEO contract, and here the escalated (Sonnet) SEO output also uses `draft`. The validator prompt gives no status vocabulary for SEO, so Haiku invents one.
- **Prompt/pipeline promise mismatch:** house_style.yaml:332 tells the analyst "the pipeline verifies limits deterministically" and :400–401 tells SEO superseded drafts "must never be" in deliverables — but every deterministic verifier (post-stage truncation, lint `seo.*_over_limit`) is `off` in llm-config.json. The prompts describe a pipeline that is not running.
- **Re-validation (haiku) verdict:** formatter FAIL ("Unattributed speakers at 10:24–10:33 … 'Speaker 1' and 'Speaker 2'"), seo PASS, overall fail. `classify_qa_failure` → artifact markers present → non-fixable → prod ends `[qa_review]`/`[qa_fail]`. **Prod outcome of the full run: PAUSED.**

### Supervisor pass to completion
- Intervention 1: formatter = single-call Sonnet 5 with `reasoning.enabled=false` (the #409 path plus LLM-1 fix). Completeness 100.3%, no seam gap, header + `Status: ready_for_editing`, no review notes.
- Intervention 2: SEO on Sonnet instead of Haiku (SEO-2). Title 75 / short 85 / long 344, all within limits, `Status: ready_for_review`. Still flagged SEO-3 (truncated excerpt, "verified" list).
- Validator (default Haiku): analyst pass, formatter pass, seo pass, **overall pass → QA gate COMPLETED.** Timestamp had already passed.
- Job complete. Repo untouched (`git status` clean); nothing written to Airtable or GitHub.
- Subagent self-report: Opus chunk0 flagged that the formatter system prompt's own example uses "John Smith (Host)" and "Host & Guest (simultaneously)" labels while the style block forbids parentheticals in labels — a prompt-internal contradiction.

### formatter (sonnet; prod default sonnet-4.6 on openrouter) — prod path = 2 chunks in parallel
- chunk0: 2000 words, chunk1: 1333 words (overlap 8 captions). Prompts replicate `_run_formatter_chunked` verbatim incl. style pre-stage section on each chunk.
- Also replayed the SINGLE-CALL prompt (what #409 would send) via OpenRouter on anthropic/claude-sonnet-5 with max_tokens=16384 for finish_reason/usage telemetry.
- Chunk outputs: chunk0 11,160 B, chunk1 7,579 B. Merged 18,679 chars.
- **Gates (prod, main):** completeness 97.9% (3262/3333) PASS; seam: no gap, net 0.9787, not blocking. No `<!-- REVIEW NOTES`, no `needs_review`. No "(section 1 of N)" leak. → prod would NOT pause here.
- **Defect (FMT-1, contract under-production in chunked path):** neither chunk emitted the `# Formatted Transcript` header block (Project/Program/Duration/Date Processed) and neither emitted `**Status:**`. Merged output therefore has NO header and NO status footer. Causes: (a) chunk-0 prompt says "Format only the dialogue in this section" right after the header-generating instruction; a modern Sonnet reads that as "no header". (b) No chunk prompt ever instructs the LAST chunk to emit the Status footer; `merge_formatter_chunks` only keeps a status line if the last chunk produced one. So the chunked path can silently produce a contract-violating document that all deterministic gates pass. This is the mirror image of job 47 (where the header leaked "section 1 of 2"). Both are chunk-wrapper bugs, not dialogue bugs.
- Chunk seam quality: chunk0 ends "The straight line." / chunk1 starts "The president, Donald Trump, decided to go to war…" — clean continuation, no echo, no drop.
- Speaker label "**Panel (simultaneously):**" used once for a group "Hello." — not in the house-style label spec.
- Speaker turns: 25 labeled turns (Shawn 13, Zac 6, Rich 4, Anya 1, Panel 1). SRT has exactly 25 `>>` markers → turn structure intact.
- **Subagent self-reports (direct evidence for FMT-1):** chunk0 agent: "system.md's CRITICAL output-format rule says every response must begin with a full '# Formatted Transcript' header … and end with a Status line, but user.md frames this call as section 1 of 2 … A per-chunk Duration and Status don't make sense for a partial section … so I omitted the header/metadata and Status line entirely." chunk1 agent: no header/status instruction at all in its prompt; also flagged honorific-abbreviation rule (Governor→Gov.) vs the VERBATIM mandate as a tension, resolved in favour of style rules; reconstructed "Ana"→"Anya" and a garbled URL from glossary; left "Arnie" (unknown name) verbatim.
